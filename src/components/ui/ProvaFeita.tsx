/** Chip indicando que a prova daquela matéria já aconteceu (clay/atenção). */
export function ProvaFeita() {
  return (
    <span className="inline-flex items-center gap-1 rounded-full border border-prova/30 bg-prova/10 px-2.5 py-0.5 text-[11px] font-bold uppercase tracking-wide text-prova">
      Prova feita
    </span>
  );
}
